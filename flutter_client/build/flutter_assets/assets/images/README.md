Place optional future visual assets here.

The current parity pass keeps rendering asset-light (emoji/procedural),
but this folder is wired in `pubspec.yaml` so PNG/SVG swaps can be added
without changing the rendering API.
